import 'package:flutter/material.dart';

class PopularStation extends StatelessWidget {
  const PopularStation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
